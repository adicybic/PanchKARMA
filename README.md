# Pancha Vitality Hub

This project contains both backend and frontend for the Pancha Vitality Hub application.

## Backend

Located in the `backend/` directory.

## Frontend

Located in the `frontend/` directory.

## Git Commands (for branching)

1) new branch: git checkout -b branchname (for ex: git checkout -b chatbot)
2) change branch to old one: git checkout existing branch name (ex: git checkout main)
